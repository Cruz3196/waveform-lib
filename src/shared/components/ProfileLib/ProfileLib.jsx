import React from 'react';
import ProfileHeader from '../ProfileInfo/ProfileHeader';

const ProfileLib = () => {
  return (
    <div>
        <ProfileHeader/>
        ProfileLib
    </div>
  )
}

export default ProfileLib;